<!DOCTYPE html>
<html lang="en">

<head>
 
</head>
<body>
  


<section class="sct-color-1 slice">
  <div class="container-fluid masonry-container">
    <div class="row">
      <div class="col-md-8 col-sm-offset-2">
        <div class="masonry-wrapper-cols">
          <div class="masonry-gutter"></div>
         <% @post_images.each do |post_image| %>
            <div class="masonry-block">
              <div class="block block-image v1">
                <div class="block-image">
                  <div class="view view-first">
                    <%= attachment_image_tag post_image, :image %>
                  </div>
                </div>
                <div class="block-content">

         <%= link_to post_image_path(post_image.id) do %>

                  <%= attachment_image_tag post_image.user, :profile_image, class: "img-circle pull-left profile-thumb" %>

                  <h2>
                  <%= post_image.title %>
                  </h2>
         <% end %>
                  


                     <i class="fa fa-map-marker"></i><%= post_image.category %>
                  <p><%= post_image.caption %></p>
                  <ul class="inline-meta">
                    <li>By
                      <%= post_image.user.name %>
                    </li>
                    <li>
                    <a href="#">23 コメント</a>
                    </li>
                  </ul>
                </div>
              </div>
            </div>
          <% end %>
          <%= paginate @post_images, class: "pagenate btn" %>
        </div>
      </div>
    </div>
  </div>
</section>

</body>
</html>
